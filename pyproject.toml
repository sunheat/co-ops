[project]
name = "co-ops"
version = "0.1.0"
description = "A universal, extensible LLM client for personal AI engineering learning."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.5.0",
]
